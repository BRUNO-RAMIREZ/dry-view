import { TamArregloObservablePipe } from './tam-arreglo-observable.pipe';

describe('TamArregloObservablePipe', () => {
  it('create an instance', () => {
    const pipe = new TamArregloObservablePipe();
    expect(pipe).toBeTruthy();
  });
});
